# configurações do banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/universidade?useTimezone=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mysql_2002

# validete -> mantém os dados que já existem na tebla, não apaga tudo do zero antes de começar
spring.jpa.hibernate.ddl-auto=validate 

# mostrar os comandos sql feitos automaticamente pelo jpa
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.format_sql=true